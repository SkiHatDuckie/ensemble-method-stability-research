# ensemble-method-stability-research

## To Use
1. Install dependencies with `pip install -e .`
2. Download the dataset with `bash prereqs.sh`
3. Run preprocessing with `python(3) -m clean_data` (from project root, not `src`)